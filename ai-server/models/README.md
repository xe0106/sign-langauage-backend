# MediaPipe task models

Model assets in this directory are local dependencies and are not committed to Git.

The preprocessing CLI currently expects:

```text
holistic_landmarker.task
```

Download the official MediaPipe Holistic Landmarker task model and pass its path with `--model`.
