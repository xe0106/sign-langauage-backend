spring:
  datasource:
    url: jdbc:mysql://sign-language-sign-language.c.aivencloud.com:23070/sign-language?ssl-mode=REQUIRED&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: avnadmin
    password: ENC(3Zw5RRI/IdEVGWvYaeC/ASVH2B3pxjdETLSMUbTAvGTa2PTCRXRfmw==)
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false

  data:
    redis:
      host: sign-language-redis-sign-language.e.aivencloud.com
      port: 23071
      username: default
      password: ENC(+a7u0e1VXhDlJn6amAmQP/14THEmA56CqtWvVrUx4Gpm9mtrKBEwdA==)
      ssl:
        enabled: true
    jpa:
      repositories:
        bootstrap-mode: default

  ## aws s3 클라우드 저장소를 통해 이미지 보관 예정
jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD}
    algorithm: PBEWithMD5AndDES

jwt:
  # 2. 커스텀 비밀키로 수정 (32자 이상)
  secret: signLanguageJwtSecretKeyForAuthentication2026SecureKey!

app:
  default-profile-image: ${DEFAULT_PROFILE_URL:https://your-s3-bucket.s3.ap-northeast-2.amazonaws.com/default-profile.png}