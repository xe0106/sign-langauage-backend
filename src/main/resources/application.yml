spring:
  datasource:
    url: jdbc:mysql://sign-language-sign-language.c.aivencloud.com:23070/sign-language?ssl-mode=REQUIRED&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: avnadmin
    password: ENC(3Zw5RRI/IdEVGWvYaeC/ASVH2B3pxjdETLSMUbTAvGTa2PTCRXRfmw==)
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false

  data:
    jpa:
      repositories:
        bootstrap-mode: default

jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD}
    algorithm: PBEWithMD5AndDES # 암호화 시 사용한 알고리즘 (기본값인 경우)
